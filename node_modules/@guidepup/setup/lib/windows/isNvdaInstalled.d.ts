import { RegistryItemValue } from "regedit";
export declare function isNvdaInstalled({ exists, userProvidedInstallDirectory, values, }: {
    exists: boolean;
    userProvidedInstallDirectory: string | null;
    values: {
        [name: string]: RegistryItemValue;
    };
}): boolean;
