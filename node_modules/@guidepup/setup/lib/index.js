"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const setup_1 = require("./macOS/setup");
const setup_2 = require("./windows/setup");
const logging_1 = require("./logging");
const errors_1 = require("./errors");
async function run() {
    try {
        switch (process.platform) {
            case "darwin": {
                await (0, setup_1.setup)();
                break;
            }
            case "win32": {
                await (0, setup_2.setup)();
                break;
            }
            default: {
                throw new Error(errors_1.ERR_UNSUPPORTED_OS);
            }
        }
    }
    catch (e) {
        (0, logging_1.handleError)(e);
    }
    (0, logging_1.handleComplete)();
}
run();
