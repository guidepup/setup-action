"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.retryOnError = retryOnError;
async function retryOnError(delegate, { retries = 3 } = {
    retries: 3,
}) {
    let error;
    for (let i = 0; i < retries; i++) {
        try {
            return await delegate();
        }
        catch (e) {
            error = e;
        }
    }
    throw error;
}
