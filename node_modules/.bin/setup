#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@guidepup/setup/bin/setup" "$@"
  ret=$?
else 
  node  "$basedir/../@guidepup/setup/bin/setup" "$@"
  ret=$?
fi
exit $ret
